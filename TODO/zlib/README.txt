install_nodl.bat
=> Modified script from "zlib.install" to skip downloading if the ZIP file is already there (+small init changes & vcvars).

zlib.install-master.zip
=> Script to automatically download+build zlib.


install
=> Generated libs+bins from build process (both debug & release)
TODO: clean duplicate files (reorganise)